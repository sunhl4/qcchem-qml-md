"""Variational algorithm plug-ins for the main pipeline.

Import concrete APIs from ``qchem_stack.quantum.variational_plugins.registry`` (e.g.
``run_variational_stage``, ``register_variational_plugin``, ``unregister_variational_plugin``)
rather than importing this ``__init__`` — it stays minimal to avoid heavy import graphs.
"""
