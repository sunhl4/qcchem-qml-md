# 切换 Backend 并对比结果

本教程帮助你在同一配置骨架下切换不同执行后端并比较结果行为。

## 对比步骤

1. 固定同一 `experiment_yaml`
2. 分别设置不同 `backend.provider`
3. 比较 `run_summary` 与资源指标

## 对比维度

- 能量估计与方差
- 运行时间与队列行为
- 资源行（深度、2Q 门、shots）

## 注意

- 后端差异会导致方差与时延分布不同
- 做对比时尽量固定随机种子和 shots 预算
