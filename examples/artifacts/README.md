# Generated artifacts

Tutorial and visualization scripts write PNG/JSON here by default (`examples/artifacts/`).

Tracked files in this directory are only `.gitkeep` and this README; other outputs are gitignored.
