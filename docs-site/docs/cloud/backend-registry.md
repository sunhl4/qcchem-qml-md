# 后端注册表

**状态**：占位骨架（Wave W0）。**目标**：文档化 **多后端**（statevector、Qiskit、IonStack、未来云模拟器）的注册与 **BackendSpec** 选择 — 对应 Vol.08「多后端注册表」原则，而非 pytket 单中心叙事。

## 建议章节（后续补全）

- 注册表 schema（名称、版本、capabilities、gateset 摘要）。
- 与 `CircuitIR` / TKET 编译路径的 **可选** 组合矩阵。
- 与 parity 矩阵中 `compiler_pass_bundle` / 采样键的交叉引用。
