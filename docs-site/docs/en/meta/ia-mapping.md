# IA slug map — English

For the live machine-readable mapping, see [`scripts/inquanto-tree.yaml`](https://github.com/your-org/qchem-stack) (the YAML manifest that drives the entire mirror) and the [Mirror index](/en/mirror/).

The four pillars and Diátaxis prefixes are defined identically across languages; only the navigation labels differ. For procurement / infosec context: [Security & data](/en/meta/security-and-data).
