# Public parity matrix

> The English version is being filled in. The authoritative content currently lives in the Chinese page: [/parity/public-matrix](/parity/public-matrix).

## At a glance

| status | meaning |
|---|---|
| shipped | A `qchem_stack` module already implements equivalent or better behavior. |
| partial | Fields/behavior exist but do not fully match InQuanto closed-source semantics. |
| placeholder | Listed in the InQuanto tree, kept as a placeholder, planned per milestone. |
| not-applicable | Out-of-scope by design (real Nexus cloud, HQC billing, H-Series hardware calibration). |

For the live, machine-readable view, see [/en/mirror/](/en/mirror/) and the underlying `inquanto-tree.yaml` manifest.
